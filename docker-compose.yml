version: '3'
services:
  mysql:
    image: mysql:latest
    container_name: mysqldb
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: devops
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - /DB:/docker-entrypoint-initdb.d/data.sql
    networks:
      - internalnet

  backend:
    image: khairislimani/devopsbackend
    container_name: devopsbackend
    ports:
      - "8082:8082"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/devops?createDatabaseIfNotExist=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
    depends_on:
      - mysql
    networks:
      - internalnet
    volumes: 
      - /DB:/app/data

  frontend:
    image: khairislimani/devopsfrontend
    container_name: devopsfrontend
    ports:
      - "4200:4200"
    depends_on:
      - backend
    networks:
      - internalnet
    
networks:
  internalnet:
    driver: bridge
